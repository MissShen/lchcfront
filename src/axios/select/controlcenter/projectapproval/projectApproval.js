import http from 'src/axios/http'

/**
 * 控制中心 --  立项
 * @param resDate
 * @param domain
 * @returns {*}
 */
  export function selectProjectApprovalList (resDate,domain) {
  return http({
    url: '',
    method: 'get',
    params: {}
  })
}
