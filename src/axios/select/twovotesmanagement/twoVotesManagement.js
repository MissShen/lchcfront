import http from 'src/axios/http'

/**
 * 签订两票制承诺书
 * @param resDate
 * @param domain
 * @returns {*}
 */
  export function selectTwoVomanagementList (resDate,domain) {
  return http({
    url: '',
    method: 'get',
    params: {}
  })
}

