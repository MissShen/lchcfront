import http from 'src/axios/http'

/**
 * 专家议价
 * @param resDate
 * @param domain
 * @returns {*}
 */
export function selectBargainExpertList (resDate,domain) {
  return http({
    url: '',
    method: 'get',
    params: {}
  })
}


