import http from 'src/axios/http'

/**
 * 企业评分
 * @param resDate
 * @param domain
 * @returns {*}
 */
export function selectScoreOrgList (resDate,domain) {
  return http({
    url: '',
    method: 'get',
    params: {}
  })
}


