import http from 'src/axios/http'

/**
 * 合同历史数据
 * @param resDate
 * @param domain
 * @returns {*}
 */
export function selectContractHistoricalDataList (resDate,domain) {
  return http({
    url: '',
    method: 'get',
    params: {}
  })
}


